 # disguise user agent
user-agent = "Mozilla/5.0 (X11; Fedora; Linux x86_64; rv:90.0) Gecko/20100101 Firefox/103.0"

# when following a redirect, automatically set the previous URL as referer.
referer = ";auto"

# wait 1 min before timing out.
connect-timeout = 60
